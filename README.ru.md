# AMDNR — DLSS 5 Neural Rendering на AMD (сборка OptiScaler) — v0.3.3.2

[English](README.md) | [中文](README.zh-CN.md) | [Português](README.pt-BR.md) | [Español](README.es.md) | [العربية](README.ar.md) | [Français](README.fr.md) | [Italiano](README.it.md) | **Русский** | [Polski](README.pl.md)

> **Нам нужна ваша поддержка.** Заходите на Discord-сервер — <https://discord.gg/AMDNR> — за
> помощью, с баг-репортами и за тестовыми сборками; каждый отчёт с логом делает следующую сборку лучше.

DLSS 5 Neural Rendering на видеокартах AMD, встроенный в OptiScaler, поэтому он работает в любой
игре на Direct3D 12, которую OptiScaler уже умеет перехватывать. Поверх нейронного прохода: model interleave
для большого прироста FPS, residual composition (остаточная композиция), генерация кадров (FG) XeSS,
разблокированная до 6X (до 10X — по желанию, в играх на D3D12), и FSR Ray Regeneration для игр,
использующих DLSS Ray Reconstruction.

**Discord: <https://discord.gg/AMDNR>** — поддержка, баг-репорты (`#bug-report`), тестовые
сборки.

**Поддержать проект: <https://ko-fi.com/3zinr>**

> **Рантайм danielblnc — это работа Daniel Blanco.** Нейронный рантайм AMD из `Runtime.zip`
> (`dlssnr_amd_pass1..3.dll`) — это **DLSS-NR on AMD by Daniel Blanco (danielblnc)** -
> <https://github.com/danielblnc/DLSS-NR-on-AMD>. Copyright (c) 2026 Daniel Blanco, all rights reserved.
> AMDNR распространяет его без изменений, с его разрешения; это не работа AMDNR. Пожалуйста, поддержите его проект.
> Полный список благодарностей всем остальным — в конце этой страницы.

---

## AMDNR - Руководство по установке OptiScaler

Установка довольно простая.

### 1. Скачайте файлы

Скачайте эти два файла с GitHub (<https://github.com/3zwr1/AMD-NR---OptiScaler/releases>):

* `AMDNR-vX.X.X.zip`
* `Runtime.zip`

### 2. Распакуйте оба файла

Распакуйте содержимое обоих файлов `.zip`.

### 3. Скопируйте всё в папку игры

Сначала скопируйте все файлы из `AMDNR-vX.X.X` в корневую папку игры — ту же папку, где
лежит `.exe` игры.

Затем сделайте то же самое со всеми файлами из `Runtime`.

### 4. Переименуйте OptiScaler.dll

В папке игры найдите:

`OptiScaler.dll`

Переименуйте его в:

`dxgi.dll`

`dxgi.dll` — рекомендуемый вариант.

Если игра не запускается или мод не загружается, попробуйте вместо этого переименовать `OptiScaler.dll`
в одно из этих имён:

* `d3d12.dll`
* `winmm.dll`
* `version.dll`
* `dbghelp.dll`

Пробуйте по одному имени за раз. Не создавайте несколько копий `OptiScaler.dll`.

> **Resident Evil Requiem (и её демо) требует REFramework.** Это известное требование, а не баг AMDNR: OptiScaler полагается
> на него, чтобы обойти anti-tamper от Capcom ([вики OptiScaler](https://github.com/optiscaler/OptiScaler/wiki/Resident-Evil-9-Requiem)). Без него игра вылетает
> через 15-60 с после запуска ("An unhandled exception occurred"). Положите `dinput8.dll` из архива `REFramework.zip` последней nightly-сборки
> (<https://github.com/praydog/REFramework-nightly/releases>) рядом с `dxgi.dll` и смените клавишу меню REFramework (например, на Delete): его меню тоже открывается по Insert.
> После обновления игры ждите вылетов, пока не обновится REFramework. PRAGMATA, Monster Hunter Wilds и Onimusha, вероятно, тоже его требуют (не подтверждено).

### 5. Запустите игру

`HOME` включает и выключает Neural Rendering прямо во время игры (для обоих рантаймов; небольшое уведомление
показывает On / Off). Переназначить клавишу можно рядом с галочкой Enable на вкладке Neural или в Interface > Keybinds.

Вот и всё.

Запустите игру как обычно и нажмите:

`INSERT`

Откроется меню OptiScaler / AMDNR, где можно настроить мод как вам угодно.

### Если не работает

Если игра так и не запускается ни с одним из имён выше, пожалуйста, сообщите об этом в канале
`#bug-report` в Discord.

К отчёту о проблеме также приложите все файлы `.log`, которые могли появиться в
корневой папке игры.

Эти логи очень важны и помогут нам найти проблему гораздо быстрее.

> `.exe` обычно лежит не там, куда указывает ярлык. В играх на Unreal он находится в
> `<Game>\Binaries\Win64\`.

---

### Рантайм lmxxf (0.3.0, опционально)

Второй нейронный рантайм (под лицензией MIT, от lmxxf) может выполнять проход вместо рантайма
danielblnc. Работает на RDNA 4, а также на RDNA 3 (RX 7000, Strix Halo) через RDNA 3-бэкенд AMDNR - там медленнее:
начните с NR resolution 67%. Рядом с игрой ему нужны две вещи:

1. `LmxxfNrRuntime.dll` - в этом архиве, рядом с `OptiScaler.dll` (копируется вместе с остальными файлами).
2. `LmxxfNrRuntime.pak` (416 МБ, входит в zip AMDNR) рядом с `LmxxfNrRuntime.dll` - файлы весов lmxxf,
   модули HIP и HLSL в одном зашифрованном файле с проверкой подлинности. Рантайм открывает его в
   памяти; на диск ничего не распаковывается.

При первом запуске, когда найден установленный рантайм, а выбор ещё не сделан, меню спрашивает, какой
из них использовать (выбор записывается в ini как `[DlssNr] NrBackend = daniel | lmxxf`; сменить его можно
в Neural > Neural runtime, изменение вступит в силу при следующем запуске игры). Правка lmxxf применяется
с опозданием на один кадр и переносится по векторам движения, так что кадр никогда не ждёт нейросеть
(около 17 мс в 1080p на RX 9070 XT). Лог lmxxf — `lmxxf_backend.log` рядом с игрой.

**Совместимость (lmxxf).** Рантайм видит только то, что видит DLSS, поэтому от игры к игре меняется
лишь короткий список: формат цвета и HDR, векторы движения и их масштаб, глубина и её направление,
reactive mask, текстура экспозиции, флаг Reset и то, где стоит проход (до Super Resolution или после
Ray Reconstruction). На данный момент проверено:

| Игра | API / положение | Примечания |
|---|---|---|
| Silent Hill 2 | D3D12, до SR | эталонная игра; учтено, что Unreal выделяет цветовой буфер с паддингом |
| Forza Horizon 6 | D3D12, до SR | |
| Stray | D3D11 через мост D3D12, до SR | |
| GTA V Enhanced | D3D12, до SR, HDR, одноканальная reactive mask | исправлено в 0.3.0: маска читалась как «всё реактивное», и правка так и не применялась |
| Любая игра с Ray Reconstruction | D3D12, после RR (записывается обратно в выходной кадр) | поддерживается с 0.3.0; в играх пока не подтверждено |

Если в игре нет эффекта: в `lmxxf_backend.log` есть строка `lmxxf inputs:` (форматы, размеры,
масштаб движения, направление глубины, маска, экспозиция) и строка `lmxxf stats @N:` каждые 600 кадров
(экспозиция, подаваемая яркость, правка модели, перенесённая правка, keep, среднее по reactive, длина
векторов и доля отброшенных). Приложите лог к отчёту; эти две строки обычно объясняют причину.

Под lmxxf в блоке Neural runtime есть **Network history** (собственный временной вход модели),
а в Image look — группа **lmxxf edit**: формирователь правки (Edit detail,
Edit colour, Edge guard: усиление мелкой части правки модели, её цвет относительно изменения её
яркости и затухание правки на границах глубины) и **Output smoothing**
(выходной проход из upstream, требует Network history). Neural passes, Residual strength/limit,
резкость (sharpening), Debug view 1 и фильтр Appearance работают под обоими рантаймами.

**Full network** (Neural > Performance, `[DlssNr] LmxxfFullNetwork`, только lmxxf) прогоняет все 71 блок
сети вместо того, чтобы пропускать 42, 43 и 46: чуть точнее, примерно на 0.5 мс медленнее в 1080p
(16.6 -> 17.1 мс на RX 9070 XT). По умолчанию выключено.

## Требования

- Видеокарта AMD с актуальным драйвером. Нейронный рантайм использует HIP через драйвер; ни HIP
  SDK, ни режим разработчика не нужны. По чипам: на RX 9000 (RDNA 4) работают оба рантайма;
  на RX 7000 (RDNA 3, десктопные и мобильные) тоже оба - lmxxf через RDNA 3-бэкенд AMDNR, медленнее,
  чем на RDNA 4; на Strix Halo (8060S / 8050S) работает lmxxf; APU портативных ПК (Z1 Extreme / 780M,
  Z2 Extreme / 890M) и RDNA 2 (RX 6000, Steam Deck) не поддерживаются ни одним из них.
  Вкладка Neural показывает, что может запустить ваша видеокарта.
- Игра на Direct3D 12, Direct3D 11 или Vulkan. Сам нейронный путь AMD работает на D3D12; игры на D3D11 и
  Vulkan попадают в него через мост D3D12 от OptiScaler, а значит, апскейлер должен быть одним из
  бэкендов "w/Dx12" (`ffx_12`). Оставьте `Dx11Upscaler` / `VulkanUpscaler` в значении `auto`,
  и эта сборка сама выберет его, когда нейронный рендеринг включён.
- Около 2 ГБ свободной VRAM при разрешениях рендера класса 1080p.

## Что лежит в двух архивах

**AMDNR-vX.X.X.zip**

| Файл | Что это |
|---|---|
| `OptiScaler.dll` | OptiScaler с AMD-бэкендом DLSS-NR. Переименуйте его, как сказано в руководстве. |
| `OptiScaler.ini` | Настройки. Neural Rendering включён; логирование включено, чтобы к баг-репорту было что приложить. |
| `LmxxfNrRuntime.dll` | Нейронный рантайм lmxxf (ядра lmxxf 0.29). Используется, только если выбран; читает `LmxxfNrRuntime.pak` рядом с собой, см. «Рантайм lmxxf». |
| `LmxxfNrRuntime.pak` | Веса, модули HIP и шейдеры рантайма lmxxf в одном зашифрованном файле (416 МБ). Его читает только рантайм lmxxf; с рантаймом danielblnc его можно спокойно оставить. |
| `OptiScaler\` | FSR, XeSS, денойзер FidelityFX и D3D12 Agility SDK, которые использует OptiScaler. |
| `OptiScaler/amdnr_dlssg_fsr3.dll` | dlssg-to-fsr3 от Nukem9, без изменений, только переименован: вызовы DLSS Frame Generation из игры обслуживаются генерацией кадров FSR 3, в том числе на Vulkan (`FGNvngxReplacement=Nukems`). GPLv3, см. `Licenses/`. |
| `Licenses\`, `LICENSE` | Лицензии сторонних компонентов, уведомление AMDNR (`AMDNR_NOTICE.txt`) и лицензия GPL-3.0 этой сборки. |
| `SHA256SUMS.txt` | Контрольные суммы всех поставляемых файлов из обоих архивов. |

**Runtime.zip**

| Файл | Что это |
|---|---|
| `dlssnr_amd_pass1..3.dll` | Нейронный рантайм AMD, v0.3.1 от danielblnc, без изменений. Три копии, чтобы в режиме multi-pass у каждого прохода была своя. |
| `dlssnr_on_amd_weights.bin` | Веса сети, которые загружает рантайм. |

## Настройки, которые стоит знать

Откройте вкладку **Neural**. Значения по умолчанию — это последняя проверенная конфигурация, поэтому
разумный первый шаг — менять что-то одно за раз.

- **NR resolution** — главный рычаг баланса между качеством и нагрузкой. Ниже 100% модель работает с картинкой
  меньшего размера, и на кадр полного разрешения переносится только её *коррекция*, поэтому
  кадр сохраняет собственную детализацию. Выше 100% нагрузка растёт квадратично (150% — это 2.25x). Ползунок
  двигается с шагом 5%: каждый новый размер NR может удерживать VRAM до перезапуска игры, поэтому
  перезапускайте игру после множества изменений.
- **Residual strength** — какая доля правки модели применяется; выше 1 правка усиливается. Именно эта
  настройка сильнее всего меняет картинку.
- **Residual limit** — потолок того, насколько сильно может измениться один пиксель. Появляются пятна — **уменьшите** его.
- **Model interleave** — запускает модель через кадр ради большого прироста FPS.
  Пропущенные кадры заполняет **Interleave preset**; *Guided fill v2* стоит по умолчанию, и именно над ним
  сейчас идёт активная работа. Пейсинг двух типов кадров настраивается автоматически, а **Adaptive
  interleave** (включён по умолчанию) запускает модель на каждом кадре, пока картинка в движении, так что
  пропуски — и их артефакты — случаются только тогда, когда картинка неподвижна.
- **Neural passes** — 2 и 3 накладывают модель несколько раз, с убывающей отдачей. Под lmxxf
  история сети остаётся за первым проходом; дополнительные проходы — только пространственное уточнение.
  danielblnc в играх на Vulkan выполняет 1 проход (об этом говорит подсказка под ползунком).
- **Colour composition** (Neural > Image look, оба рантайма) — *Classic* (по умолчанию) даёт ту картинку,
  что была у вас раньше. *RenoDX (experimental)* выполняет цветовую композицию RenoDX после модели, как
  это делает путь NVIDIA: Composition detail и colour, двусторонний **Highlight guard** (2x по умолчанию),
  который ограничивает ответ модели относительно оригинала, и необязательные настройки кожи / окружения.
  На display-referred (SDR) кадре этот режим откатывается к Classic, о чём сообщает пометка в меню. Стили NR
  и пресеты его не трогают.
- **Генерация кадров (FG) в новом ini выключена.** Вкладка Frame Gen: выберите FG Input (например, "DLSSG via
  Streamline" в игре с генерацией кадров DLSS) и FG Output (XeFG), затем отметьте **Active** в
  разделе Frame Generation (XeFG) и нажмите Save Settings. Если в ini от 0.1.0 она была включена, это
  не переносится при установке ini от 0.2.0.
- **Мультикадровая генерация XeFG** — от 3X до 6X встроено и включено по умолчанию (`XeFG\UnlockMFG`),
  как для копии OptiScaler, так и для собственной копии игры. **Удалите `XeFGUnlock.asi`** из `OptiScaler\plugins`,
  если он у вас остался: две копии одного и того же патча приводят к вылету игры.
  **До 10X — включается вручную** (только в играх на D3D12): задайте *XeFG ceiling (restart)* в разделе FG Output
  на вкладке Frame Gen (4X, 6X по умолчанию, 8X или 10X; `[XeFG] MaxInterpolatedFrames`), перезапустите игру,
  затем выберите множитель в списке MFG. Выше 6X нужен собственный провайдер XeFG от OptiScaler с включённым
  Extra pacing; копия XeSS 3, поставляемая с игрой, остаётся максимум на 6X. Для 10X нужен дисплей 360 Гц+ и
  ограничение кадров на уровне частоты обновления / 10; задержка высокая, а провайдер резервирует примерно
  на 128 МиБ больше VRAM в 4K. 7X-10X в игре пока не подтверждены: тестеры, пришлите, пожалуйста, `OptiScaler.log`.
- **FSR Ray Regeneration** — по умолчанию только на RDNA 4 (RX 9000); только в играх с DLSS Ray Reconstruction (Cyberpunk 2077,
  Alan Wake 2), при этом игра должна работать на DLSS (spoofing включён), а трассировка лучей и Ray
  Reconstruction должны быть включены в её собственных настройках. Тогда Neural Rendering выполняется после
  него, на его выходе, что обходится дороже: снизьте NR resolution, если FPS проседает. Его настройки
  (Neural > Quality > Ray Regeneration) появляются, только пока игра использует Ray Reconstruction.
  **Профиль path-traced** (меньше зерна на лицах при path tracing) с 0.3.3.1 включается вручную: отметьте его
  там, чтобы попробовать в Resident Evil Requiem или PRAGMATA. Там же находятся сила bias mask, отладочный
  вид RR и **сглаживание кожи** (экспериментально, для игр, которые предоставляют SSS guide; по умолчанию
  выключено, но в Resident Evil Requiem с 0.3.3.2 включено по умолчанию).

## Если что-то пошло не так

`OptiScaler.log` появляется в папке игры. Приложите его в `#bug-report` и укажите игру и
видеокарту. AMD-бэкенд также пишет `amd_presr.log` и `amd_bridge.log` — именно они полезны,
когда сбоит конкретно нейронный проход. Лог предыдущей сессии сохраняется как
`OptiScaler.previous.<exe>.log`; после вылета приложите и его (в новом логе тогда будет "no clean exit
recorded").

**NR frames 0/s, а вкладка Neural или `amd_presr.log` сообщает, что DLL прохода — это сборка, с которой этот
AMDNR не работает?** Ваши `dlssnr_amd_pass1..3.dll` — сборка danielblnc, которую этот AMDNR не знает (в сети
попадался набор 0.2.16), или одного из трёх файлов не хватает. Начиная с 0.3.3.2 вкладка Neural называет файл
и его версию и подсказывает, что делать. Используйте `v0.4.0-Runtime.zip` (самый новый) или `Runtime.zip` (0.3.1)
из этого релиза, все три DLL прохода из одного и того же zip: `dlssnr_amd_pass1.dll` в `v0.4.0-Runtime.zip`
весит 10,027,008 байт, SHA256 начинается с `d62be3d8`. Поддерживаемые сборки: 0.2.17, 0.3.0, 0.3.1, 0.3.2, 0.3.3,
0.4.0, а также 0.4.1 / 0.4.2 заранее, до их выхода. Не устанавливайте собственный установщик danielblnc и его
`dxgi.dll` / `version.dll` / `winhttp.dll` рядом с AMDNR: AMDNR уже запускает его рантайм.

**lmxxf ничего не делает или сразу останавливается на ПК со встроенной графикой?** Исправлено в 0.3.3.2.
На десктопном Ryzen с включённой встроенной графикой, на ноутбуке с APU AMD и Radeon или на ПК с двумя
видеокартами AMD видеокарта, на которой идёт игра, часто не является HIP-устройством 0. Тогда lmxxf падал
на первом же кадре (`hipErrorInvalidHandle (400)`, затем "session is poisoned" в `lmxxf_backend.log`) и
оставался выключенным. Замените и `OptiScaler.dll` (файл, который вы переименовали, например `dxgi.dll`), и
`LmxxfNrRuntime.dll` файлами из 0.3.3.2. На таком ПК это пока не тестировалось: если lmxxf всё равно
останавливается, вкладка Neural теперь объясняет причину; пришлите `lmxxf_backend.log` и `amd_bridge.log`
(в нём перечислены HIP-устройства).

**Игра на Vulkan (Indiana Jones and the Great Circle) останавливается при запуске с ошибкой "Could not create
the Vulkan device (VK_ERROR_EXTENSION_NOT_PRESENT)"?** Исправлено в 0.3.2: унаследованный нейронный путь
NVIDIA запрашивал у драйвера AMD два расширения устройства, которые есть только у NVIDIA. Игры на Vulkan
попадают в нейронный проход через мост D3D12 от OptiScaler (см. «Требования»).

**lmxxf вешал игру на Vulkan на первом кадре NR?** Исправлено в 0.3.3; при старте NR ожидайте один
фриз примерно на 1 с. Если сессия на Vulkan когда-нибудь остановится до первого ответа lmxxf, при
следующем запуске будет работать рантайм danielblnc, а вкладка Neural объяснит почему; нажмите там
**Retry lmxxf** (это удаляет `lmxxf_vk_launch.pending` рядом с `OptiScaler.dll`), чтобы снова попробовать lmxxf.

**danielblnc подвисал на несколько секунд, а затем отключал NR в игре на Vulkan (Indiana Jones) с 2-3 Neural
passes?** Исправлено в 0.3.3: в играх на Vulkan он выполняет 1 проход, а его ожидание 80 мс после отправки
(post-submit) убрано. Первый кадр NR в сессии по-прежнему даёт паузу около 5 с; подсказка под выбором рантайма
объясняет соответствующие строки лога. Тестерам: `[DlssNr] AmdVkLateCopyWait=true` (экспериментально, по
умолчанию выключено, в игре пока не проверено) должно убрать эту паузу; присылайте `OptiScaler.log`,
`amd_presr.log` и `dlssnr_on_amd.log`.

**Потребление RAM у lmxxf росло всё время, пока работал NR?** Исправлено в 0.3.3 (было около 45 ГБ в час
при 60 кадрах NR в секунду). Что осталось: danielblnc удерживает VRAM для каждого нового размера NR больше
примерно 1 Мп (в 0.3.3.2 его размеры вне 100% округляются до 64 px, так что таких размеров совсем немного);
с danielblnc перезапускайте игру после множества изменений. Начиная с 0.3.3.2 lmxxf больше не удерживает около
97 МБ на каждое изменение NR resolution или режима DLSS: он создаёт буферы сети один раз для каждого размера
сети и затем использует их повторно (на каждое изменение остаётся лишь небольшой остаток — около 10-25 МБ VRAM).

**Игра на Streamline не запускается с ошибкой slInit 0x18 (замечено в NBA 2K27 на AMD)?** В 0.3.3 закрыт
один из путей, которым её могли вызывать хуки OptiScaler на плагины Streamline, но не подтверждено, что
причина в NBA 2K27 именно в этом. В `OptiScaler.log` теперь пишутся строки `slInit returned ...` и `[SLINIT]`:
пришлите лог вместе с отчётом.

**Ray Reconstruction в игре включён, но вкладка Neural пишет "Ray Regeneration is off in this title"?**
Игра не отдаёт то, что нужно FSR Ray Regeneration (Satisfactory: нет матриц камеры). Вместо него работает
апскейлинг FSR, а NR занимает своё обычное место до SR; никакие настройки в ini этого не меняют.

**Игра на движке Ubisoft Anvil (AC Black Flag Resynced, Shadows, Mirage) показывает "DX12 Error 0x80070057"?**
В этих играх есть собственная генерация кадров XeSS. Эта сборка оставляет её самой игре (выход XeFG от OptiScaler
там отключается, и вкладка Frame Gen об этом сообщает); используйте встроенную в игру опцию XeSS FG. Если
ошибка всё равно появляется, задайте `[FrameGen] Enabled=false` и `[fakenvapi] ForceXeLL=false` и отправьте
отчёт с логом.

**The Last of Us Part I вылетает при запуске?** Это собственная инициализация Streamline в игре, известная
проблема OptiScaler: переименуйте `sl.common.dll` в папке игры в `sl.common.dll.bak` и выберите
**FSR 3.1** в настройках игры вместо DLSS.

Полные заметки по каждой версии: `CHANGELOG.md` (в zip и в репозитории).

## Дорожная карта

- **0.3.3** (эта сборка) — lmxxf на RDNA 3 (RX 7000; собственный бэкенд AMDNR); цветовая композиция
  RenoDX (экспериментально, включается вручную) на обоих рантаймах; lmxxf: опция Full network, исправлена
  утечка RAM, исправлены игры на Vulkan (ленивая загрузка весов внутри моста Vulkan), ядра 0.29 (побитово
  идентичные, быстрее); danielblnc в играх на Vulkan: 1 Neural pass, более понятные сообщения, опциональное
  позднее ожидание копирования; XeFG до 10X (по желанию, D3D12); более надёжный запуск Streamline и
  диагностика; профиль path-traced и сглаживание кожи для FSR Ray Regeneration; повышенная надёжность в UE5.
- **0.3.2** — по отчётам о 0.3.1: игры на Vulkan запускаются и работают с lmxxf, цвета lmxxf
  приведены к цветам danielblnc (автоэкспозиция), выпадающий список рантаймов, статус и настройка Ray
  Reconstruction; dlssg-to-fsr3 от Nukem9 в zip для генерации кадров на Vulkan.
- **0.3.1** — исправления по первым отчётам о 0.3.0 (lmxxf в одиночку вообще не запускался, «тихий» NR
  в Where Winds Meet, вылет при смене качества DLSS, GTA V Legacy) и пресеты стилей NR с тремя
  пользовательскими слотами.
- **0.3.0** — нейронный HIP-рантайм **lmxxf** (RDNA 4) как выбираемый рантайм наряду с рантаймом
  danielblnc, поставляется как `LmxxfNrRuntime.dll` + `LmxxfNrRuntime.pak`: история сети,
  настоящие Neural passes, формирователь правки, размещение после Ray Regeneration, диагностика
  для каждой игры и самовосстановление. Огромное спасибо TheAutomatic, на чьей работе над проектом
  DLSS 5 AMD построена эта интеграция.
- **0.4.0** — AMDNR Launcher (установка рантаймов и pak в один клик, обновления) и поддержка игр
  без собственного апскейлера (вроде Stray), где OptiScaler сам предоставляет и апскейлер, и
  нейронный проход.

---

## Благодарности

Эта сборка — по сути обвязка поверх чужой работы. Если она вам пригодилась, благодарить
стоит авторов исходных проектов (upstream).

- **TheAutomatic** — DLSS 5 AMD project — https://github.com/TheAutomatic/dlss-5-amd-project
- **danielblnc** — DLSS-NR on AMD — https://github.com/danielblnc/DLSS-NR-on-AMD (`Runtime.zip`, без изменений)
- **lmxxf** — https://github.com/lmxxf/dlss5-on-amd-9070xt-porting (HIP-рантайм, MIT)
- **ядра c32w** (0.3.3.2) — собственные ядра AMDNR для RDNA 4, работающие в одну волну (one wave), для сети lmxxf, Copyright (c) 2026 3zwr1 (AMDNR); идеи взяты из открытой документации AMD по WMMA на RDNA 4 (GPUOpen, ROCm matrix instruction calculator)
- **Matheus / dlss-5-amd** — https://github.com/MatheusGViana/dlss-5-amd-project
- **Nukem9** — dlssg-to-fsr3 — https://github.com/Nukem9/dlssg-to-fsr3 (GPLv3, без изменений)
- **RenoDX** — clshortfuse — https://github.com/clshortfuse/renodx (математика цветовой композиции, MIT)
- **Coldwood1026** — XeFGUnlock (GPL-3.0), основа встроенной разблокировки мультикадровой генерации XeFG и её пейсинга
- **burak113** — препроцессор FSR Ray Regeneration (ветка OptiScaler ffx-denoise-experimental, GPL-3.0)
- **OptiScaler** — Overclockers — https://github.com/Overclockers/OptiScaler-Releases

## Авторские права / Лицензия

AMDNR — Copyright (c) 2026 3zwr1 (AMDNR). Это форк OptiScaler, распространяемый по лицензии GPL-3.0
из файла `LICENSE`.

На собственную работу AMDNR распространяется дополнительное условие по разделу 7(b) GPL-3.0 (см.
`Licenses/AMDNR_NOTICE.txt`): любая копия, форк или производная работа, которая её использует, должна
сохранять её уведомления и указывать авторство **AMDNR by 3zwr1** (<https://github.com/3zwr1/AMD-NR---OptiScaler>).

**Авторские права на меню AMDNR.** Меню AMDNR — его компоновка, дизайн, тексты и код, добавленный для него AMDNR, — Copyright (c) 2026 3zwr1 (AMDNR). Оно является частью этого форка под GPL-3.0 со следующими дополнительными условиями (GPL-3.0 section 7): (b) любой, кто повторно использует любую его часть, обязан сохранить эту строку об авторских правах и на видном месте указать авторство AMDNR by 3zwr1 — в меню и в README; (c) нельзя выдавать его или его изменённую копию за свою собственную работу; изменённые версии должны быть чётко помечены как изменённые; (e) никакие права на название или логотип AMDNR не предоставляются; другие проекты не вправе их использовать.

Работа upstream, указанная выше, остаётся за её авторами и под их собственными лицензиями; AMDNR не
претендует на авторские права на неё.

Исходный код будет опубликован вместе с AMDNR 0.5.0.

## Правовая информация

Эта сборка распространяется по лицензии GPL-3.0 из файла `LICENSE`; лицензии сторонних библиотек
находятся в `Licenses\`. Нейронный рантайм AMD и его веса распространяются повторно с сохранением
исходного авторства, как указано выше, исключительно для удобства, без каких-либо притязаний на
владение и без каких-либо гарантий.

`nvngx_dlssnr.dll` от NVIDIA в этих архивах нет. Ничто из этого не одобрено NVIDIA, AMD или каким-либо
издателем игр, не связано с ними и не поддерживается ими. Сборка напрямую управляет недокументированной
функцией. Используйте на свой страх и риск.
